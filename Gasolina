Algoritmo "Gasolina"

Var

valor, qtd, total: real

Inicio
valor <- 2.40
escreval("Informe a quantidade que precisa colocar de gasolina:")
leia(qtd)
total <- 0
total <-( valor * qtd)

escreval("O valor da gasolina é: ", valor, " você colocou: ", qtd, " total: ", total)



Fimalgoritmo
